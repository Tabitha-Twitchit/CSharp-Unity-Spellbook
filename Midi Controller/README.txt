Your integration of the EasyController midicontroller package. Included here is both the standalone script 
as well as an exported package with all dependencies. EasyController must be setup on the target editing machine.
Specific setup instructions are within the summary in the script itself. Images in this folder show how to setup
and configure the game object in hierarchy and inspector.

